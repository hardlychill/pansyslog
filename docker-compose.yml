services:
  pansyslog:
    build: .
    container_name: pansyslog
    restart: unless-stopped
    ports:
      - "8787:8787"
    volumes:
      - pansyslog-data:/data
      - ./config.yaml:/etc/pansyslog/config.yaml:ro
    environment:
      - PAN_PASS=${PAN_PASS}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  vector:
    image: timberio/vector:0.44.0-debian
    container_name: pansyslog-vector
    restart: unless-stopped
    ports:
      - "5514:5514/udp"
    volumes:
      - ./configs/vector-docker.yaml:/etc/vector/vector.yaml:ro
      - pansyslog-data:/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  dashboard:
    build: ./dashboard
    container_name: pansyslog-dashboard
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - pansyslog-data:/data:ro
    environment:
      - PANSYSLOG_API=http://pansyslog:8787
      - DATA_DIR=/data
    depends_on:
      - pansyslog
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  pansyslog-data:
